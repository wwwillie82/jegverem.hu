# Force /allasajanlat slug through the root front controller.
# Place inside the active server block for jegverem.hu/www.jegverem.hu.

location = /allasajanlat {
    rewrite ^ /index.php last;
}

location = /allasajanlat/ {
    rewrite ^ /index.php last;
}
